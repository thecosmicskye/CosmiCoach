//
//  MultipeerService+UnifiedAccess.swift
//  MultiPeerDemo
//
//  Created by Claude on 3/22/25.
//

import Foundation
import MultipeerConnectivity

// This extension ensures that all the functions in separate files
// can be accessed throughout the codebase
extension MultipeerService {
    // MARK: - Enums and Types

        // No need to redefine types here as they're already defined in the main class
    // and we're in an extension, so they're already accessible
}